<%= form_for(@newborn) do |f| %>
  <% if @newborn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newborn.errors.count, "error") %> prohibited this newborn from being saved:</h2>

      <ul>
      <% @newborn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_of_birth %><br>
    <%= f.date_select :date_of_birth %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number_of_weeks %><br>
    <%= f.text_field :number_of_weeks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
